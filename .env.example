# Backend API URL — set this in Railway environment variables
# after the T3 backend service is deployed
VITE_API_URL=https://your-backend-service.up.railway.app

# Set to 'production' on Railway
NODE_ENV=production
